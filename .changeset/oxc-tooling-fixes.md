---
"@ngrok/mantle": patch
---

### Bug Fixes

- `AlertDialog.Header` and `AlertDialog.Footer` now correctly forward the `ref` prop
- `CommandEmpty` now accepts and applies the `className` prop

### Changes

- Updated tailwind selectors to be new, preferred syntax
- `DataTable.Header`: Removed unused generic type parameters `<TData, TValue>`
