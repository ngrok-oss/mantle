---
"@ngrok/mantle": patch
---

Replace core fonts with new brand fonts: Roobert (sans), JetBrains Mono (mono), and Family (prose). Updates `PreloadCoreFonts` to preload the new font files and changes font type to woff2.
