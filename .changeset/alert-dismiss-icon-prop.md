---
"@ngrok/mantle": patch
---

Add optional `icon` prop to `Alert.DismissIconButton`.

The dismiss button now accepts an `icon` prop (`ReactNode`) to render a custom icon in place of the default `X` icon.
