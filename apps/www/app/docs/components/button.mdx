---
title: Button
description: Initiates an action, such as completing a task or submitting information.
---

import { Button } from "@ngrok/mantle/button";
import { GlobeHemisphereWestIcon } from "@phosphor-icons/react/GlobeHemisphereWest";
import { Link, href } from "react-router";
import { Example } from "~/components/example";

# Button

Initiates an action, such as completing a task or submitting information.

<Example className="flex flex-wrap gap-6">
	<div>
		<p className="mb-2 text-center font-mono text-xs">Neutral</p>
		<div className="flex items-center gap-2">
			<Button type="button" appearance="ghost" intent="neutral">
				Ghost
			</Button>
			<Button type="button" appearance="filled" intent="neutral">
				Filled
			</Button>
			<Button type="button" appearance="outlined" intent="neutral">
				Outlined
			</Button>
			<Button type="button" appearance="subtle" intent="neutral">
				Subtle
			</Button>
			<Button type="button" appearance="link" intent="neutral">
				Link
			</Button>
		</div>
	</div>
	<div>
		<p className="mb-2 text-center font-mono text-xs">Accent</p>
		<div className="flex items-center gap-2">
			<Button type="button" appearance="ghost" intent="accent">
				Ghost
			</Button>
			<Button type="button" appearance="filled" intent="accent">
				Filled
			</Button>
			<Button type="button" appearance="outlined" intent="accent">
				Outlined
			</Button>
			<Button type="button" appearance="subtle" intent="accent">
				Subtle
			</Button>
			<Button type="button" appearance="link" intent="accent">
				Link
			</Button>
		</div>
	</div>
	<div>
		<p className="mb-2 text-center font-mono text-xs">Danger</p>
		<div className="flex items-center gap-2">
			<Button type="button" appearance="ghost" intent="danger">
				Ghost
			</Button>
			<Button type="button" appearance="filled" intent="danger">
				Filled
			</Button>
			<Button type="button" appearance="outlined" intent="danger">
				Outlined
			</Button>
			<Button type="button" appearance="subtle" intent="danger">
				Subtle
			</Button>
			<Button type="button" appearance="link" intent="danger">
				Link
			</Button>
		</div>
	</div>
</Example>

```tsx
import { Button } from "@ngrok/mantle/button";

<Button type="button" intent="neutral">Outlined</Button>
<Button type="button" intent="neutral" appearance="filled">Filled</Button>
<Button type="button" intent="neutral" appearance="ghost">Ghost</Button>
<Button type="button" intent="neutral" appearance="subtle">Subtle</Button>
<Button type="button" intent="neutral" appearance="link">Link</Button>

<Button type="button" intent="accent">Outlined</Button>
<Button type="button" intent="accent" appearance="filled">Filled</Button>
<Button type="button" intent="accent" appearance="ghost">Ghost</Button>
<Button type="button" intent="accent" appearance="subtle">Subtle</Button>
<Button type="button" intent="accent" appearance="link">Link</Button>

<Button type="button" intent="danger">Outlined</Button>
<Button type="button" intent="danger" appearance="filled">Filled</Button>
<Button type="button" intent="danger" appearance="ghost">Ghost</Button>
<Button type="button" intent="danger" appearance="subtle">Subtle</Button>
<Button type="button" intent="danger" appearance="link">Link</Button>
```

## Size

Use the `size` prop to control the height and padding of the button. The default size is `md`.

<Example className="flex-col gap-4">
	<div className="flex items-center gap-2">
		<Button type="button" size="sm">
			Small
		</Button>
		<Button type="button" size="md">
			Medium
		</Button>
		<Button type="button" size="lg">
			Large
		</Button>
		<Button type="button" size="xl">
			Extra Large
		</Button>
	</div>
	<div className="flex items-center gap-2">
		<Button type="button" size="sm" icon={<GlobeHemisphereWestIcon />}>
			Small
		</Button>
		<Button type="button" size="md" icon={<GlobeHemisphereWestIcon />}>
			Medium
		</Button>
		<Button type="button" size="lg" icon={<GlobeHemisphereWestIcon />}>
			Large
		</Button>
		<Button type="button" size="xl" icon={<GlobeHemisphereWestIcon />}>
			Extra Large
		</Button>
	</div>
</Example>

```tsx
import { Button } from "@ngrok/mantle/button";
import { GlobeHemisphereWestIcon } from "@phosphor-icons/react/GlobeHemisphereWest";

<Button type="button" size="sm">Small</Button>
<Button type="button" size="md">Medium</Button>
<Button type="button" size="lg">Large</Button>
<Button type="button" size="xl">Extra Large</Button>

<Button type="button" size="sm" icon={<GlobeHemisphereWestIcon />}>Small</Button>
<Button type="button" size="md" icon={<GlobeHemisphereWestIcon />}>Medium</Button>
<Button type="button" size="lg" icon={<GlobeHemisphereWestIcon />}>Large</Button>
<Button type="button" size="xl" icon={<GlobeHemisphereWestIcon />}>Extra Large</Button>
```

## Icon and Positioning

Use the `icon` prop to add an icon to the button. By default, it will render on the logical start side of the button. Use the `iconPlacement` prop to change the side the icon is rendered on.

<Example>
	<div className="flex items-center gap-2">
		<Button type="button" icon={<GlobeHemisphereWestIcon />}>
			Icon Start
		</Button>
		<Button type="button" icon={<GlobeHemisphereWestIcon />} iconPlacement="end">
			Icon End
		</Button>
	</div>
</Example>

```tsx
import { Button } from "@ngrok/mantle/button";
import { GlobeHemisphereWestIcon } from "@phosphor-icons/react/GlobeHemisphereWest";

<Button type="button" icon={<GlobeHemisphereWestIcon />}>Icon Start</Button>
<Button type="button" icon={<GlobeHemisphereWestIcon />} iconPlacement="end">
	Icon End
</Button>
```

## isLoading

`isLoading` determines whether or not the button is in a loading state, default `false`. Setting `isLoading` will replace any `icon` with a spinner, or add one if an icon wasn't given. It will also disable user interaction with the button and set `aria-disabled`.

<Example className="flex-col gap-4">
	<div className="space-y-2">
		<p className="mb-2 text-center font-mono text-xs">Idle</p>
		<div className="flex flex-wrap items-center justify-center gap-2">
			<Button type="button">No Icon + Idle</Button>
			<Button type="button" icon={<GlobeHemisphereWestIcon />}>
				Icon Start + Idle
			</Button>
			<Button type="button" icon={<GlobeHemisphereWestIcon />} iconPlacement="end">
				Icon End + Idle
			</Button>
		</div>
	</div>
	<div className="space-y-2">
		<p className="mb-2 text-center font-mono text-xs">isLoading</p>
		<div className="flex flex-wrap items-center justify-center gap-2">
			<Button type="button" isLoading>
				No Icon + isLoading
			</Button>
			<Button type="button" icon={<GlobeHemisphereWestIcon />} isLoading>
				Icon Start + isLoading
			</Button>
			<Button type="button" icon={<GlobeHemisphereWestIcon />} iconPlacement="end" isLoading>
				Icon End + isLoading
			</Button>
		</div>
	</div>
</Example>

```tsx
import { Button } from "@ngrok/mantle/button";
import { GlobeHemisphereWestIcon } from "@phosphor-icons/react/GlobeHemisphereWest";

<Button type="button">No Icon + Idle</Button>
<Button type="button" icon={<GlobeHemisphereWestIcon />}>Icon Start + Idle</Button>
<Button type="button" icon={<GlobeHemisphereWestIcon />} iconPlacement="end">
	Icon End + Idle
</Button>
<Button type="button" isLoading>No Icon + isLoading</Button>
<Button type="button" icon={<GlobeHemisphereWestIcon />} isLoading>
	Icon Start + isLoading
</Button>
<Button type="button" icon={<GlobeHemisphereWestIcon />} iconPlacement="end" isLoading>
	Icon End + isLoading
</Button>
```

## Composition

When you want to render _something else_ as a `Button`, you can use the `asChild` prop to compose. This is useful when you want to splat the `Button` styling onto a `react-router` `Link`. Keep in mind that when you use `asChild` the `type` prop will **NOT** be passed to the child component.

<Example>
	<Button appearance="filled" icon={<GlobeHemisphereWestIcon />} asChild>
		<Link to={href("/base/colors")}>See our colors!</Link>
	</Button>
</Example>

```tsx
import { Button } from "@ngrok/mantle/button";
import { GlobeHemisphereWestIcon } from "@phosphor-icons/react/GlobeHemisphereWest";
import { Link, href } from "react-router";

<Button appearance="filled" icon={<GlobeHemisphereWestIcon />} asChild>
	<Link to={href("/base/colors")}>See our colors!</Link>
</Button>;
```

## API Reference

### Button

All props from [button](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attributes), plus:

| Prop             | Type                                                              | Default      | Description                                                                                                                                                                                                                                                                      |
| ---------------- | ----------------------------------------------------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `appearance?`    | `"ghost"` \| `"filled"` \| `"outlined"` \| `"subtle"` \| `"link"` | `"outlined"` | Defines the visual style of the `Button`.                                                                                                                                                                                                                                        |
| `asChild?`       | `boolean`                                                         | `false`      | Use the `asChild` prop to compose the `Button` styling onto alternative element types or your own React components.                                                                                                                                                              |
| `icon?`          | `ReactNode`                                                       |              | An icon to render inside the button. When `isLoading` is `true`, the icon will automatically be replaced with a spinner.                                                                                                                                                         |
| `iconPlacement?` | `"start"` \| `"end"`                                              | `"start"`    | The side that the icon will render on, if one is present. When `isLoading` is `true`, the loading spinner will also render on this side.                                                                                                                                         |
| `isLoading?`     | `boolean`                                                         | `false`      | Determines whether or not the button is in a loading state. Setting `isLoading` will replace any `icon` with a spinner, or add one if an icon wasn't given. It will also disable user interaction with the button and set `aria-disabled`.                                       |
| `intent?`        | `"accent"` \| `"danger"` \| `"neutral"`                           | `"neutral"`  | Indicates the importance or impact level of the button, affecting its color and styling to communicate its purpose to the user.                                                                                                                                                  |
| `size?`          | `"sm"` \| `"md"` \| `"lg"` \| `"xl"`                              | `"md"`       | The size of the `Button`.                                                                                                                                                                                                                                                        |
| `type`           | `"button"` \| `"reset"` \| `"submit"`                             |              | The default behavior of the `Button`. Unlike the native `button` element, unless you use the `asChild` prop, **this prop is required and has no default value**. See [the MDN docs](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#type) for more information. |
